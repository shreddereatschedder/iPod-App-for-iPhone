name: Build iPodApp

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode 15.4
        run: sudo xcode-select -s /Applications/Xcode_15.4.app

      - name: Install XcodeGen 2.38.0
        run: |
          brew unlink xcodegen 2>/dev/null || true
          brew install xcodegen@2.38.0 2>/dev/null || brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate --spec project.yml

      - name: Check generated project format
        run: grep "objectVersion" iPodApp.xcodeproj/project.pbxproj | head -3

      - name: Resolve Swift packages
        run: xcodebuild -resolvePackageDependencies -project iPodApp.xcodeproj -scheme iPodApp

      - name: Build app
        run: |
          xcodebuild \
            -project iPodApp.xcodeproj \
            -scheme iPodApp \
            -configuration Debug \
            -sdk iphonesimulator \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: iPodApp-build
          path: build/Build/Products/Debug-iphonesimulator/iPodApp.app
          retention-days: 7