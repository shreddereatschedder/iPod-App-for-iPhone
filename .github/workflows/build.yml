name: Build iPodApp

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode 15.4
        run: sudo xcode-select -s /Applications/Xcode_15.4.app

      - name: Check Xcode version
        run: xcodebuild -version

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Check XcodeGen version
        run: xcodegen --version

      - name: Generate Xcode project
        run: xcodegen generate --spec project.yml

      - name: Print objectVersion
        run: grep "objectVersion" iPodApp.xcodeproj/project.pbxproj

      - name: Print archiveVersion
        run: grep "archiveVersion" iPodApp.xcodeproj/project.pbxproj

      - name: Resolve Swift packages
        run: xcodebuild -resolvePackageDependencies -project iPodApp.xcodeproj -scheme iPodApp

      - name: Build app
        run: |
          xcodebuild \
            -project iPodApp.xcodeproj \
            -scheme iPodApp \
            -configuration Debug \
            -sdk iphonesimulator \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: iPodApp-build
          path: build/Build/Products/Debug-iphonesimulator/iPodApp.app
          retention-days: 7